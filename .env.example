# Optional: override default CLI commands
# GEMINI_CLI_CMD="gemini -p {prompt}"
# CLAUDE_CLI_CMD="claude --print --output-format text {prompt}"
# CODEX_CLI_CMD="codex exec --skip-git-repo-check --sandbox read-only {prompt}"
